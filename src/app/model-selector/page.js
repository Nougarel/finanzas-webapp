/**
 * Ruta: /model-selector
 * Página de selección del modelo financiero — primer paso del flujo directo
 */
import ModelSelectorPage from "@/components/pages/ModelSelectorPage";

export default ModelSelectorPage;
