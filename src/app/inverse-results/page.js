/**
 * Ruta: /inverse-results
 * Página de resultados del cálculo inverso — muestra el ingreso mensual necesario
 */
import InverseResultsPage from "@/components/pages/InverseResultsPage";

export default InverseResultsPage;
